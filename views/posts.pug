extends layout

block content
    each post in posts
        div(class=['card'])
            div(class=['card-body'])
                h1(class=['card-title']) #{post.title}
                div(class=['card-text'])
                    != post.html
